# Локальная разработка: react-scripts + volume ./frontend:/app
FROM node:18-alpine

WORKDIR /app

COPY package*.json ./
RUN npm ci --silent

COPY . .

ENV HOST=0.0.0.0
ENV WDS_SOCKET_PORT=0
ENV CHOKIDAR_USEPOLLING=true
ENV API_PROXY_TARGET=http://backend:8000

EXPOSE 3000

CMD ["npm", "start"]
